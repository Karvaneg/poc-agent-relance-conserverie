# Infra IA — Claude Code
# Le contexte est regenere a la demande via scripts/generate-ai-context.sh
.claude/skills/project-context.md

# Secrets / variables d'environnement
.env
.env.*
!.env.example

# Environnements virtuels Python
.venv/
venv/

# Bytecode / cache Python
__pycache__/
*.pyc

# Docker / donnees locales
docker-compose.override.yml
*.local.yml
data/
*.db
*.sqlite
*.sqlite3

# Rapports HTML generes (livrables, non versionnes)
out/

# Contexte de mission (offre d'emploi, e-mail du cas pratique) — local uniquement,
# contient des donnees personnelles : ne jamais pousser sur le depot public.
docs/contexte-mission/
